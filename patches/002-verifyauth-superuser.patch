diff --git a/internal/site/src/lib/api.ts b/internal/site/src/lib/api.ts
index 79f4b12..995adab 100644
--- a/internal/site/src/lib/api.ts
+++ b/internal/site/src/lib/api.ts
@@ -13,7 +13,10 @@ export const isAdmin = () => pb.authStore.record?.role === "admin"
 export const isReadOnlyUser = () => pb.authStore.record?.role === "readonly"
 
 export const verifyAuth = () => {
-	pb.collection("users")
+	// [beszel patch] We run on a superuser token (auto-auth from the Hermes
+	// plugin backend). authRefresh on "users" would 404 → logOut() → login page
+	// loop. Validate via _superusers instead.
+	pb.collection("_superusers")
 		.authRefresh()
 		.catch(() => {
 			logOut()
